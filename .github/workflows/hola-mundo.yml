name: hola-mundo
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Saludo
        run: echo "Hola, Mundo!"
      - name: Despedida
        run: node main.js